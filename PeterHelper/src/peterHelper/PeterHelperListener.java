package peterHelper;

import org.bukkit.event.Listener;

public class PeterHelperListener implements Listener
{
	private PeterHelper plugin;
	
	public PeterHelperListener(PeterHelper plugin)
	{
		this.plugin=plugin;
	}
}
